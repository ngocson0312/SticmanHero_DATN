import Foundation
import UIKit
import StoreKit

@objc public class MyGameAdOpeniOSSwift : NSObject {
    
    @objc public static let shared = MyGameAdOpeniOSSwift()
    
    @objc public func Adload(viewController: UIViewController) {
//         let myWebView:UIWebView = UIWebView(frame: CGRect(x:0, y:0, width: UIScreen.main.bounds.width, height:UIScreen.main.bounds.height))
        
//         viewController.view.addSubview(myWebView)
// //        myWebView.delegate = self
        
//         if let filepath = Bundle.main.path(forResource: "index", ofType: "html") {
//             do {
//                 let contents = try String(contentsOfFile: filepath)
//                 DispatchQueue.main.async{
//                     myWebView.loadHTMLString(contents, baseURL: nil)
//                 }
                
//             } catch {
//                 // contents could not be loaded
//             }
//         } else {
//             // example.txt not found!
//         }
        
    }
}
